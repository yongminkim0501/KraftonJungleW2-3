"""
[재귀 - 하노이의 탑 (Tower of Hanoi)]

▣ 문제 배경
1883년 프랑스 수학자 Édouard Lucas 가 고안한 고전 퍼즐로, 저작권이 만료되어
자유롭게 사용/배포할 수 있는 퍼블릭 도메인 문제입니다. 본 지문 및 테스트
케이스 또한 본 학습 자료를 위해 자체적으로 작성되었습니다.

▣ 상황 설명
3개의 기둥이 있고, 각 기둥의 번호는 1, 2, 3 입니다.
1번 기둥에는 서로 다른 크기의 원반 N 개가 큰 것이 아래에 오도록 쌓여 있습니다.

    초기 상태 (N = 3 예시)
      [3]
      [2]
      [1]
      ─┬─    ───    ───
       1      2      3

▣ 목표
1번 기둥에 있는 모든 원반을 3번 기둥으로 옮기는 절차를 구하세요.

▣ 규칙
  (1) 한 번에 정확히 하나의 원반만 다른 기둥으로 옮길 수 있다.
  (2) 큰 원반이 작은 원반 위에 놓이는 상황은 절대 만들 수 없다.

▣ 작은 예시 (N = 2)
    초기:  [2,1] |  []  |  []      (1번에 큰 원반 2, 그 위에 작은 원반 1)
    1) 1 -> 2 :  [2]  | [1] |  []
    2) 1 -> 3 :  []   | [1] |  [2]
    3) 2 -> 3 :  []   | []  |  [2,1]
    총 3번의 이동이며, 이는 최소이고 정확히 2^2 - 1 = 3 입니다.

▣ 구현할 함수
1) hanoi_count(n: int) -> int
   - N 개의 원반을 옮기는 데 필요한 "최소 이동 횟수" 를 정수로 반환합니다.
   - 수학적으로 답은 정확히 2^N - 1 입니다.
   - 0 <= N <= 1000 까지 정확한 정수로 반환해야 합니다.
     (파이썬 int 는 임의 정밀도이므로 자연스럽게 가능합니다.)

2) hanoi_moves(n: int) -> list[tuple[int, int]]
   - 1번 기둥에서 3번 기둥으로 옮기는 이동 순서를
     (출발기둥, 도착기둥) 튜플의 리스트로 반환합니다. 기둥 번호는 1, 2, 3.
   - 이동 횟수가 폭발적으로 늘어나는 것을 방지하기 위해
     N > 20 또는 N == 0 인 경우에는 빈 리스트 [] 를 반환해야 합니다.

▣ 제약
- 0 <= N <= 1000 (hanoi_count)
- 0 <= N <= 20  (hanoi_moves 는 이 범위에서만 실제 시퀀스를 반환)

▣ 힌트 (재귀 아이디어)
    move(k, src, via, dst):
        if k == 0: return
        move(k-1, src, dst, via)        # 위쪽 k-1개를 via 로
        결과 리스트에 (src, dst) 추가   # 가장 큰 원반을 dst 로
        move(k-1, via, src, dst)        # via 의 k-1개를 dst 로
"""


def hanoi_count(n: int) -> int:
    """N 개의 원반을 옮기는 데 필요한 최소 이동 횟수( = 2^N - 1) 를 반환"""
    # TODO: 2^N - 1 을 정수로 반환하세요.
    pass


def hanoi_moves(n: int) -> list:
    """
    N <= 20 일 때 1번 -> 3번 기둥으로 옮기는 이동 순서를 리스트로 반환.
    각 원소는 (출발기둥, 도착기둥) 튜플이며, 기둥 번호는 1/2/3 의 정수.
    N > 20 또는 N == 0 인 경우에는 빈 리스트 [] 를 반환합니다.

    예) hanoi_moves(2) == [(1, 2), (1, 3), (2, 3)]
    """
    # TODO: N > 20 또는 N == 0 인 경우 [] 를 반환하세요.
    # TODO: 그 외에는 재귀로 이동 순서를 만들어 반환하세요.
    pass


if __name__ == "__main__":
    print("[테스트 1] N=0 (원반 없음, 옮길 것 없음)")
    print(f"  최소 이동 횟수: {hanoi_count(0)}")
    print(f"  이동 순서: {hanoi_moves(0)}")
    print()

    print("[테스트 2] N=1 (원반 1개, 1번 -> 3번 한 번)")
    print(f"  최소 이동 횟수: {hanoi_count(1)}")
    print(f"  이동 순서: {hanoi_moves(1)}")
    print()

    print("[테스트 3] N=3 (실제 이동 순서까지 출력)")
    print(f"  최소 이동 횟수: {hanoi_count(3)}")
    print("  이동 순서:")
    for s, d in hanoi_moves(3):
        print(f"    {s} -> {d}")
    print()

    print("[테스트 4] N=20 (이동 순서 반환의 상한)")
    print(f"  최소 이동 횟수: {hanoi_count(20)}")
    print(f"  이동 순서 개수: {len(hanoi_moves(20))}")
    print()

    print("[테스트 5] N=100 (큰 수 확인, 이동 순서는 미생성)")
    print(f"  최소 이동 횟수: {hanoi_count(100)}")
    print(f"  이동 순서 개수: {len(hanoi_moves(100))}")
